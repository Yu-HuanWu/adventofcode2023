const fs = require('fs');

fs.readFile('input', 'utf8', (err, data) => {
    if (err) {
        console.error(err);
        return;
    }

    // part 1

    data.split("\n\n").forEach((datum, idx) => {

    });

});